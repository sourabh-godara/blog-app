import BlogCard from '@/components/blog-card'
import React from 'react'

export default async function page({ params }) {
  return (
    <>
      <main>{/*  <BlogCard blogs={allBlogs} /> */}</main>
    </>
  )
}
